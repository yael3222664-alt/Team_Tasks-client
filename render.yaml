services:
  - type: web
    name: TaskTeam-dashboard
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist/TaskTeam
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
